#------------------------------------------------------------------------------#
# Application YAML #
# inside ./src/main/resources/application.yml
#------------------------------------------------------------------------------# 
demo:
  teams:
    api: http://localhost:9090/teams
spring:
## Chapter 17 Authorization Server
  security:
    oauth2:
      client:
        registration:
          demo-client:
            client-id: client
            client-secret: secret
            # scope: events.read 
            authorization-grant-type: client_credentials
            client-name: Calendar Client
        provider:
          demo-client:
            token-uri: http://localhost:7070/oauth2/token
server:
  port: 8888
